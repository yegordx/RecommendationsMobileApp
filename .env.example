# Backend API base URL.
# Use http://localhost:5241 for the Android emulator or iOS simulator on the same machine.
# Use http://<your-wifi-ip>:5241 when running Expo Go on a physical phone (must be reachable from the device).
EXPO_PUBLIC_API_URL=http://localhost:5241
